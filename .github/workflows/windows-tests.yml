name: Windows tests

on:
  push:
    branches: [master]
  pull_request:
  workflow_call:
    inputs:
      ref:
        description: Commit to test; empty uses the pull request merge ref
        type: string
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && inputs.ref == '' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' && inputs.ref == '' }}

jobs:
  publish-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Restore node_modules
        id: node-modules-cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci
      - name: Test native PowerShell publishing wrapper
        run: npm test -- --runInBand tests/unit/agentMode/skills/builtin/openArtifactsPublishWrappers.test.ts
      - name: Verify sanitization without regexp optimization
        run: node --no-regexp-optimization node_modules/jest/bin/jest.js --runInBand --runTestsByPath tests/unit/openArtifacts/openArtifactsDocument.test.ts tests/unit/utils/redactLog.test.ts
  test-windows:
    name: test-windows (shard ${{ matrix.shard }}/4)
    runs-on: windows-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Restore node_modules
        id: node-modules-cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
      - name: Restore Jest transform cache
        uses: actions/cache@v4
        with:
          path: .jest-cache
          key: ${{ runner.os }}-jest-${{ matrix.shard }}-${{ hashFiles('package-lock.json', 'jest.config.js', 'jest.setup.js', 'tsconfig.json') }}-${{ hashFiles('src/**', 'tests/**', 'dev/**', 'scripts/**') }}
          restore-keys: |
            ${{ runner.os }}-jest-${{ matrix.shard }}-${{ hashFiles('package-lock.json', 'jest.config.js', 'jest.setup.js', 'tsconfig.json') }}-
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci
      - name: Run remaining native Windows unit suite
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path windows-test-results
          git rev-parse HEAD > windows-test-results/environment.txt
          [System.Environment]::OSVersion.VersionString >> windows-test-results/environment.txt
          node --version >> windows-test-results/environment.txt
          npm.cmd --version >> windows-test-results/environment.txt
          node -p "process.platform + ' ' + process.arch" >> windows-test-results/environment.txt
          node node_modules/jest/bin/jest.js --shard=${{ matrix.shard }}/4 --cacheDirectory=.jest-cache --silent --testPathIgnorePatterns=openArtifactsPublishWrappers.test.ts --json --outputFile=windows-test-results/jest-${{ matrix.shard }}.json *> windows-test-results/jest-${{ matrix.shard }}.log
          $testExitCode = $LASTEXITCODE
          if ($testExitCode -ne 0) {
            Get-Content windows-test-results/jest-${{ matrix.shard }}.log
          }
          exit $testExitCode
      - name: Retain native test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-unit-node-22-shard-${{ matrix.shard }}
          path: windows-test-results/
          retention-days: 14
          compression-level: 0
